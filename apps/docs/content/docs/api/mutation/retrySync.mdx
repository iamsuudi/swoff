---
title: retrySync
description: Force retry of all failed mutations.
---

## `retrySync()`

From `swoff/mutation/sync.ts`. Re-processes all mutations marked as `failed`.

```ts
import { retrySync } from "swoff/mutation/sync";
```
