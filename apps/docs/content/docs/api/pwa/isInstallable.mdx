---
title: isInstallable
description: Check if PWA can be installed.
---

## `isInstallable()`

From `swoff/pwa/prompt.ts`. Returns `boolean`.

```ts
import { isInstallable } from "swoff/pwa/prompt";
```
