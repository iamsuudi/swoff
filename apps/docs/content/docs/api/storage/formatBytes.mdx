---
title: formatBytes
description: Format byte values to human-readable string.
---

## `formatBytes(bytes)`

From `swoff/storage.ts`. Formats bytes to KB, MB, GB.

```ts
import { formatBytes } from "swoff/storage";

formatBytes(1024); // "1 KB"
```
