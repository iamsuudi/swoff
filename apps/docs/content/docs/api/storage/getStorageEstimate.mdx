---
title: getStorageEstimate
description: Estimate used and available storage.
---

## `getStorageEstimate()`

From `swoff/storage.ts`. Returns `Promise<StorageEstimate>` via `navigator.storage.estimate()`.

```ts
import { getStorageEstimate } from "swoff/storage";
```
