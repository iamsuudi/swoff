---
title: Framework Integration
description: Integrate Swoff into any project — quick-start and per-framework config.
icon: Cable
---

# Framework Integration

## Quick Start

The same 3 steps work for any stack:

```bash
npx @swoff/cli init
# Edit swoff.config.json to enable features
npx @swoff/cli generate
```

Step 4 depends on your framework:

**Bundler frameworks (React, Vue, Svelte, Astro, Next.js, Remix):**

```ts
import { initServiceWorker } from "./swoff/client-injector";
initServiceWorker();
```

**No bundler (vanilla, Laravel, Django, Flask, Rails, Go, HTMX):**

```html
<script src="/swoff/client-injector.bundle.js"></script>
```

```bash
# 5. Build the service worker (run after your build)
node swoff/sw/generator.js
```

Node.js projects: add to `package.json`:

```json
{
  "scripts": {
    "build": "vite build && node swoff/sw/generator.js"
  }
}
```

**Framework-specific differences:**

- **`swoff.config.json` values** — `build.outputDir`, `navigation.mode`, `fallback`, `precacheDirs`, `ignorePatterns`
- **Adapters** — Bundlers get hooks; others use window events or headers

See per-ecosystem pages:

| Page | Ecosystems | Adapters |
| ---- | ---------- | -------- |
| [React](/docs/frameworks/react) | Vite, Next.js, Remix, TanStack Start | Generated hooks |
| [Astro](/docs/frameworks/astro) | Astro | Generated hooks |
| [Vue](/docs/frameworks/vue) | Vite, Nuxt | Blueprint |
| [Svelte](/docs/frameworks/svelte) | Vite, SvelteKit | Blueprint |
| [Laravel / PHP](/docs/frameworks/laravel) | Laravel, PHP | Window events |
| [Django & Flask](/docs/frameworks/django) | Django, Flask | Window events |
| [Rails](/docs/frameworks/rails) | Rails | Window events |
| [Go](/docs/frameworks/go) | net/http, chi, gin | Window events |
| [HTMX](/docs/frameworks/htmx) | HTML-over-wire | Header-based |
| [Vanilla HTML](/docs/frameworks/vanilla) | Plain HTML, SSG | Window events |
| [Blueprint](/docs/frameworks/blueprint) | Any framework | Create your own |
