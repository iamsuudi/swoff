---
title: Navigation Caching
description: SPA/SSR navigation modes, preload, fallback chain, precache routes.
icon: Map
---

How Swoff handles page navigation — what happens when a user clicks a link or types a URL.

## Preconditions

- Service worker registered and controlling the page

## Status

**Already on by default.** After `swoff init`, navigation mode is `"spa"` with preload enabled.

## How it works

Navigation requests go through the **same strategy pipeline** as any other fetch (see [Caching Strategies](/docs/guides/caching-strategy)). The strategy — cache-first, network-first, or whichever — is determined by URL pattern matching, not by whether the request is a navigation. **Only the strategy decides whether to hit the network or serve from cache.**

Each strategy calls two navigation-aware helper functions during its pipeline:

1. **`serveFromCache()`** — looks up a cached response. The navigation mode (`NAV_MODE`) determines what counts as a hit for navigation requests, and whether a precached fallback path can serve as a catch-all.
2. **`fallback()`** — runs only when the strategy exhausted all options (network failed and cache returned null). It uses `NAV_MODE` to decide which caches and fallback levels to try.

## serveFromCache

Called whenever a strategy checks the cache. The lookup depends on whether the request is a navigation.

### Navigation requests

Each mode follows its own lookup chain:

| Mode      | Steps                                                                               |
| --------- | ----------------------------------------------------------------------------------- |
| `spa`     | If `fallback` path configured and found in precache → serve it. Otherwise → `null`. |
| `ssr`     | Precache → runtime-html cache → `null`.                                             |
| `default` | Same as ssr.                                                                        |

### Non-navigation requests (JS, CSS, API)

Same regardless of `NAV_MODE`:

1. Runtime cache → hit? serve.
2. Precache (content-type must match `Accept` header) → hit? serve.
3. `null`.

No branch decides to hit the network — each returns a cached response or `null`. The **strategy** decides what to do next.

## Fallback chain

When the strategy exhausted all options (network failed + `serveFromCache` returned null), `fallback()` runs. The mode determines which levels are tried before the inline HTML 503:

| Mode      | Fallback steps                                                                |
| --------- | ----------------------------------------------------------------------------- |
| `spa`     | precache → per-route rules → inline 503                                       |
| `ssr`     | precache → runtime-html → per-route rules → global fallback path → inline 503 |
| `default` | Same as ssr                                                                   |

## Navigation modes

| Mode        | serveFromCache for navs                                                          | Fallback chain                                       | When to use                                                                                |
| ----------- | -------------------------------------------------------------------------------- | ---------------------------------------------------- | ------------------------------------------------------------------------------------------ |
| `"spa"`     | Checks precached fallback path. Returns `null` if none configured or not cached. | Skips runtime-html and global fallback (steps 2, 4). | Single-page apps (React, Vue, Svelte). Client router handles routing.                      |
| `"ssr"`     | Checks precache → runtime-html. Returns `null` if neither matches.               | Full chain (all 5 levels).                           | Server-rendered apps (Next.js pages, Laravel, Rails). Repeat visits can serve cached HTML. |
| `"default"` | Same as ssr.                                                                     | Full chain (all 5 levels).                           | Static sites or mixed content.                                                             |

## Config reference

```json
{
  "features": {
    "serviceWorker": {
      "navigation": {
        "mode": "spa",
        "preload": true,
        "fallback": "/offline",
        "precacheRoutes": [],
        "rules": []
      }
    }
  }
}
```

### mode

`"spa"`, `"ssr"`, or `"default"`. Controls serveFromCache and fallback behavior as described above.

### preload

When `true`, the SW enables Navigation Preload during activation. On the next navigation, the browser starts the network request in parallel with SW startup. The SW races the preload response against a short timeout (half the fetch timeout). If the preload wins, it's used as the primary response and cached. This shaves off one round-trip time on the first navigation after activation.

Set to `false` if you don't want the SW to preload navigations.

### fallback

The URL path of an HTML page to serve as the offline fallback. What happens with it depends on mode:

- **SPA mode**: If `serveFromCache()` finds this path in precache, it serves it for any navigation request. This is the typical SPA offline pattern — serve the app shell and let the client router show an offline message.
- **SSR/default mode**: Used only in `fallback()` after everything else has been tried.

Empty string means no fallback (browser shows its own offline page).

### precacheRoutes

URL paths to fetch and cache during SW install, so they work offline from the first visit:

```json
"precacheRoutes": ["/", "/about", "/offline"]
```

These are stored in the precache (`CACHE_NAME`) and are checked by `serveFromCache` for SSR/default nav requests and by `fallback()` in all modes.

### rules

Per-route fallback paths, checked only inside `fallback()`:

```json
"rules": [
  { "match": "/blog/**", "fallback": "/offline-blog" },
  { "match": "/admin/*", "fallback": "/login" }
]
```

When a navigation request to `/blog/post-123` fails (offline + no cache), `matchRouteFallback` matches the URL against each rule's glob pattern and returns the corresponding `fallback` path from precache. Rules are checked after precache and runtime-html but before the global fallback path.

They do **not** affect normal cached operation — they only apply when the network and cache both fail to produce a response.

## Framework adapters

See the [Framework Integration](/docs/frameworks) page for adapter usage in your ecosystem. React adapters are generated by the CLI into `swoff/adapters/`. Import and use them as needed:

```ts
import { useNetworkStatus } from "./swoff/adapters/useNetworkStatus";
import { useOfflineAnalytics } from "./swoff/adapters/useOfflineAnalytics";
```

## Related

- [Caching strategies: patterns, timeout, reactive](/docs/guides/caching-strategy)
- [Data fetching: fetchWithCache, prefetchCache](/docs/guides/data-fetching)
- [Config reference: navigation](/docs/cli/config#featuresserviceworkernavigation)
