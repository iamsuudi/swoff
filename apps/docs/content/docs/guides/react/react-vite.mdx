---
title: React + Vite
description: Set up Swoff with React + Vite.
icon: Atom
---

# React + Vite

Concise adapter for **React + Vite** projects.

## Create Project

```bash
npm create vite@latest my-app -- --template react-ts
cd my-app && npm install
```

## Project Structure

```
my-app
├── src
│   ├── main.tsx
│   └── sw-template.js
├── generate-sw.js
├── sw.d.ts
└── package.json
```

## Entry Point (`src/main.tsx`)

Add SW registration to React's entry point:

```typescript
import React from 'react'
import ReactDOM from 'react-dom/client'
import App from './App'

async function registerSW() {
  const { version } = await fetch('/version.json').then(r => r.json())
  await navigator.serviceWorker.register(`/sw-v${version}.js`)
}

if ('serviceWorker' in navigator) {
  window.addEventListener('load', registerSW)
}

ReactDOM.createRoot(document.getElementById('root')!).render(<App />)
```

## Add Patterns

Copy these files from [Patterns](/docs/patterns):

1. **[SW Template](/docs/patterns/sw-template)** → `src/sw-template.js`
2. **[Build Script](/docs/patterns/build-scripts)** → `generate-sw.js`
3. **[Client Registration](/docs/patterns/client-registration)** → `src/sw-registration.js` (enhanced registration with version checking)
4. **[TypeScript Declarations](/docs/patterns/utilities)** → `sw.d.ts`

Update `package.json`:
```json
{
  "scripts": {
    "build": "vite build && node generate-sw.js"
  }
}
```

## Build & Test

```bash
npm version patch
npm run build
```

Test offline: DevTools → Application → Service Workers → Check "Offline".

## Next Steps

- [React Hooks](/docs/patterns/update-hooks) — `usePWAUpdate`, `useSWProgress`
- [Components](/docs/patterns/components) — `UpdatePrompt`, `SWProgressBar`
- [Core Architecture](/docs/core/offline-first) — Understand the patterns
