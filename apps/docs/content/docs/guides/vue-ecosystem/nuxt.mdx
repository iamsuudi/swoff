---
title: Nuxt
description: Set up Swoff with Nuxt 3 (client-only mode).
icon: nuxt
---

## Nuxt

Adapter for **Nuxt 3** with client-only mode. Swoff runs client-only — Nuxt server features are not used.

## Create Project

```bash
npx nuxi@latest init my-app
cd my-app && npm install
```

## Configure Client-Only

```typescript
// nuxt.config.ts
export default defineNuxtConfig({
  ssr: false, // Required for offline-first
});
```

## Project Structure

```
my-app
├── plugins
│   └── sw.client.ts
├── swoff
│   ├── sw-template.js
│   ├── sw-generator.js
│   ├── sw-injector.js
│   └── swoff.d.ts
└── package.json
```

## Entry Point (`plugins/sw.client.ts`)

```typescript
// plugins/sw.client.ts
export default defineNuxtPlugin(() => {
  if ("serviceWorker" in navigator) {
    window.addEventListener("load", async () => {
      const { version } = await fetch("/version.json").then((r) => r.json());
      await navigator.serviceWorker.register(`/sw-v${version}.js`);
    });
  }
});
```

## Add Patterns

Copy from [Patterns](/docs/patterns):

1. **[SW Template](/docs/patterns/sw-template)** → `swoff/sw-template.js`
2. **[Build Script](/docs/patterns/build-scripts)** → `swoff/sw-generator.js`
3. **[Client Registration](/docs/patterns/client-registration)** → `swoff/sw-injector.js`
4. **[TypeScript Declarations](/docs/patterns/client-registration#typescript-declarations)** → `swoff/swoff.d.ts`

## Output Directory

Nuxt outputs to `.output/public/` by default. Update your `swoff/sw-generator.js`:

```javascript
writeFileSync(join(__dirname, "..", ".output", "public", `sw-v${pkg.version}.js`), sw);
writeFileSync(join(__dirname, "..", ".output", "public", "version.json"), JSON.stringify({ ... }));
```

If deploying to Vercel, Netlify, or Cloudflare, the output path may differ.

Update `package.json`:

```json
{
  "scripts": {
    "build": "nuxt build && node swoff/sw-generator.js"
  }
}
```

## Build & Test

```bash
npm version patch
npm run build
```

## Next Steps

- [Vue Composables](/docs/guides/vue-ecosystem/composables) — all generated composables
- [Vue Components](/docs/guides/vue-ecosystem/components) — `UpdatePrompt`, `SWProgressBar`
