---
title: Vue + Vite
description: Set up Swoff with Vue + Vite.
icon: Vue
---

# Vue + Vite

Concise adapter for **Vue 3 + Vite** projects.

## Create Project

```bash
npm create vite@latest my-app -- --template vue-ts
cd my-app && npm install
```

## Project Structure

```
my-app
├── src
│   ├── main.ts
│   └── sw-template.js
├── generate-sw.js
├── sw.d.ts
└── package.json
```

## Entry Point (`src/main.ts`)

Add SW registration to Vue's entry point:

```typescript
import { createApp } from 'vue'
import App from './App.vue'

async function registerSW() {
  const { version } = await fetch('/version.json').then(r => r.json())
  await navigator.serviceWorker.register(`/sw-v${version}.js`)
}

if ('serviceWorker' in navigator) {
  window.addEventListener('load', registerSW)
}

createApp(App).mount('#app')
```

## Add Patterns

Copy these files from [Patterns](/docs/patterns):

1. **[SW Template](/docs/patterns/sw-template)** → `src/sw-template.js`
2. **[Build Script](/docs/patterns/build-scripts)** → `generate-sw.js`
3. **[Client Registration](/docs/patterns/client-registration)** → `src/sw-registration.js`
4. **[TypeScript Declarations](/docs/patterns/utilities)** → `sw.d.ts`

Update `package.json`:
```json
{
  "scripts": {
    "build": "vite build && node generate-sw.js"
  }
}
```

## Build & Test

```bash
npm version patch
npm run build
```

## Next Steps

- [Vue Composables](/docs/patterns/update-hooks) — Adapt `usePWAUpdate` for Vue
- [Components](/docs/patterns/components) — `UpdatePrompt`, `SWProgressBar`
- [Core Architecture](/docs/core/offline-first) — Understand the patterns
