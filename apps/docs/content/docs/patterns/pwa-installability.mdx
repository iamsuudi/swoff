---
title: PWA & Installability
description: Configure your app as a PWA with install prompts.
icon: Download
---

## Prerequisites

1. **HTTPS or localhost** — Required for SW and PWA
2. **Service Worker active** — Already set up via [SW Template](/docs/patterns/sw-template) → [Build Scripts](/docs/patterns/build-scripts) → [Client Registration](/docs/patterns/client-registration)
3. **Web App Manifest** — Describes your app

## Setup with CLI

If you're using the CLI, the PWA install handler is generated automatically when `features.pwa` is `true`:

```bash
npx @swoff/cli generate
# Generates: swoff/pwa-install.js, public/manifest.json
```

### Configuring Install Behavior

Control whether the browser's native install prompt shows naturally or is suppressed:

```json title="swoff.config.json"
{
  "features": {
    "pwa": true
  },
  "pwa": {
    "preventDefaultInstall": false
  }
}
```

| `preventDefaultInstall` | Behavior |
|------------------------|----------|
| `false` (default) | Browser shows native prompt naturally. Event is captured for manual triggering. |
| `true` | Browser prompt is suppressed. Dev must call `promptInstall()` manually. |

## Manual Setup

### 1. Create Manifest

Create `public/manifest.json`:

```json
{
  "name": "My Offline App",
  "short_name": "MyApp",
  "start_url": "/",
  "display": "standalone",
  "background_color": "#ffffff",
  "theme_color": "#000000",
  "icons": [
    { "src": "/icons/icon-192.png", "sizes": "192x192", "type": "image/png" }
  ]
}
```

### 2. Link in HTML

```html
<link rel="manifest" href="/manifest.json" />
<meta name="theme-color" content="#000000" />
```

### 3. Handle Install Prompt

```typescript
window.addEventListener("beforeinstallprompt", (e) => {
  e.preventDefault();
  window.deferredInstallPrompt = e as any;
});

async function promptInstall() {
  await window.deferredInstallPrompt?.prompt();
  const { outcome } = await window.deferredInstallPrompt.userChoice;
}
```

### iOS

iOS Safari does not fire the `beforeinstallprompt` event. Detect and show manual install instructions:

```javascript
function detectPWAInstallability() {
  const isIOS = /iphone|ipad|ipod/i.test(navigator.userAgent);

  if (isIOS) {
    showIOSInstallInstructions();
    return;
  }

  window.addEventListener("beforeinstallprompt", (e) => {
    e.preventDefault();
    window.deferredInstallPrompt = e;
  });
}

function showIOSInstallInstructions() {
  // Guide users to: Share → Add to Home Screen
  console.log("iOS: Use Share menu → Add to Home Screen");
}
```

### 4. Using the Generated Handler

If you generated `swoff/pwa-install.js` via the CLI:

```javascript
import { isInstallable, promptInstall } from './swoff/pwa-install.js';

// Listen for installable event
window.addEventListener('pwa-installable', (e) => {
  console.log('PWA can be installed:', e.detail.isInstallable);
  // Show your custom install button
});

// When user clicks install button
async function onInstallClick() {
  const result = await promptInstall();
  console.log('User choice:', result);
}
```

### 5. Detect PWA Mode

```typescript
function isRunningAsPWA(): boolean {
  return (
    window.matchMedia("(display-mode: standalone)").matches ||
    (navigator as any).standalone === true
  );
}
```

### 6. Generate Icons

Create icons at 192x192 and 512x512 minimum.

## Test PWA

1. DevTools → Application → Manifest
2. Verify manifest loads
3. Check install icon in address bar
4. Test offline mode

## Checklist

- [ ] Manifest created and linked
- [ ] Icons generated (192x192, 512x512)
- [ ] SW registered (see [Client Registration](/docs/patterns/client-registration))
- [ ] Offline works
- [ ] Install prompt works

## Next Steps

- Understand the difference: [Service Workers](/docs/concepts/what-is-offline-and-sw#versioned-app-vs-pwa-install) — versioning vs PWA install
- See [PWA Explained](/docs/concepts/pwa-and-browser-apis) — what PWA actually is
- Add data storage: [IndexedDB Patterns](/docs/patterns/indexeddb-patterns)
- Follow your [Framework Guide](/docs/guides/) — integration examples
