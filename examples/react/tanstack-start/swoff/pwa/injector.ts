/**
 * Swoff PWA Injector
 * Re-exports setupPwaInstall from the prompt module.
 *
 * Usage:
 *   import { setupPwaInstall } from './swoff/pwa/injector.ts';
 *   setupPwaInstall();
 */

export { setupPwaInstall } from "./prompt.ts";
